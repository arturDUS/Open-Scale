#include "Recipe.h"

/*
 * struct sRecipeBomElement {
  uint8_t recipe;
  uint8_t bompos;
  char name[MAX_NAME_LENGTH];
  float quantity;
  float density;
  float tolerance;
  char unit[5];
};
 */

std::vector<sRecipeBomElement> MBR;

uint8_t insertBOMPos(uint8_t recipe, uint8_t bompos, char *name, float quantity, float density, float tolerance, char *unit){


}
